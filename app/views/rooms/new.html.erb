<div class="guide-room">
  <%= form_with model: @room, local: true do |f| %>
  <div class="guide-room-button">


    <% @admins.each do |admin| %>
    <input name="room[admin_ids][]" type="hidden" value=<%= admin.id %>>
    <% end %>
    <%= f.submit 'チャットを開始する', class: "guide-room-btn" %>
  </div>
  <% end %>
</div>